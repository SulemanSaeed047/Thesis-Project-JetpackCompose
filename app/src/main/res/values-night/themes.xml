<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for the dark theme -->
    <style name="Base.Theme.EcomViews" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here -->
        <item name="colorPrimary">@android:color/black</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@android:color/darker_gray</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorTertiary">@android:color/black</item>
        <item name="colorOnTertiary">@android:color/white</item>
        <!-- Define more colors and customizations for the dark theme -->
    </style>

    <!-- Final theme that will be used in your app -->
    <style name="Theme.EcomViews" parent="Base.Theme.EcomViews" />
</resources>
